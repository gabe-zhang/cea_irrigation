[project]
name = "cea-irrigation"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Yuan Zhang", email = "yuanzzhang3@gmail.com" }
]
requires-python = ">=3.13.12"
dependencies = [
    "pyserial>=3.5",
]

[project.scripts]
cea-irrigation = "cea_irrigation:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
